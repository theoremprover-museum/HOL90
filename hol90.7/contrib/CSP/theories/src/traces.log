

          HHH                 LL
          HHH                  LL
          HHH                   LL
          HHH                    LL
          HHH          OOOO       LL
          HHHHHHH     OO  OO       LL
          HHHHHHH     OO  OO       LLL
          HHH          OOOO        LLLL
          HHH                     LL  LL
          HHH                    LL    LL
          HHH                   LL      LL
          HHH                  LL        LL90.7

Created on Tue Nov 15 10:19:01 GMT 1994
using: Standard ML of New Jersey, Version 0.93, February 15, 1993


val it = () : unit
val it = () : unit
- [opening traces.sml]

Declaring theory "traces".

Theory "CSP_base" already consistent with disk, hence not exported.
val it = () : unit
val it = () : unit
val it = () : unit
val APPEND_NIL = |- !l. APPEND l [] = l : thm
val APPEND_ID = |- !l l'. (l = APPEND l l') = l' = [] : thm
val it = () : unit
val trace = (--`(ty_antiq((==`:'a list`==)))`--) : term
val TR_STRICT = |- !f. TR_STRICT f = f [] = [] : thm
val TR_DIST = |- !f. TR_DIST f = (!s t. f (APPEND s t) = APPEND (f s) (f t))
  : thm
val TR_DIST_STRICT = |- !f. TR_DIST f ==> TR_STRICT f : thm
val ITERATE =
  |- (!t. ITERATE 0 t = []) /\
     (!n t. ITERATE (SUC n) t = APPEND t (ITERATE n t)) : thm
val ITER_COMM = |- !n t. ITERATE (SUC n) t = APPEND (ITERATE n t) t : thm
val ITER_APP =
  |- !n s t.
       ITERATE (SUC n) (APPEND s t) =
       APPEND s (APPEND (ITERATE n (APPEND t s)) t) : thm

Theory "traces" exported.
val it = () : unit
val it = () : unit
Theory: traces

Parents:
    list_lib1
    CSP_base

Type constants:
    

Term constants:
    TR_STRICT (Prefix)   :('a list -> 'a list) -> bool
    TR_DIST (Prefix)   :('a list -> 'a list) -> bool
    ITERATE (Prefix)   :num -> 'a list -> 'a list

Axioms:
    

Definitions:
    TR_STRICT |- !f. TR_STRICT f = f [] = []
    TR_DIST |- !f. TR_DIST f = (!s t. f (APPEND s t) = APPEND (f s) (f t))
    ITERATE
    |- (!t. ITERATE 0 t = []) /\
       (!n t. ITERATE (SUC n) t = APPEND t (ITERATE n t))

Theorems:
    TR_DIST_STRICT |- !f. TR_DIST f ==> TR_STRICT f
    ITER_COMM |- !n t. ITERATE (SUC n) t = APPEND (ITERATE n t) t
    ITER_APP
    |- !n s t.
         ITERATE (SUC n) (APPEND s t) =
         APPEND s (APPEND (ITERATE n (APPEND t s)) t)

Theory "traces" is consistent with disk.
val it = () : unit
