#!/bin/sh

if test -z "$THEORY_TYPE"; then THEORY_TYPE=ascii; fi

mkdir theories/$THEORY_TYPE 2>/dev/null || true
$* < theories/src/build.sml

# Old - didn't work
#echo 'use_and_exit print_HOL_ERR "theories/src/mk_integer.sml";' | $*
