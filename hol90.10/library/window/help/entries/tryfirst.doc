\DOC tryfirst

\TYPE {tryfirst : (('a -> 'b) -> 'a list -> 'b)}

\SYNOPSIS
The first element of a list for which a function succeeds.

\LIBRARY window

\DESCRIBE
{tryfirst f l} returns {f e} where {e} if the first element
of {l} for which {f} succeeds.

\FAILURE
Fails for {f} fails for every element of {l}.

\ENDDOC
