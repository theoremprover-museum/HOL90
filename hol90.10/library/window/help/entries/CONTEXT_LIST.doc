\DOC CONTEXT_LIST

\TYPE {CONTEXT_LIST : ((thm list -> win_stack -> win_stack) -> win_stack -> win_stack)}

\SYNOPSIS
Supply the context to a window transformation function.

\LIBRARY window

\DESCRIBE
If {f} is a function that takes a list of theorems and transforms a
window stack, then {CONTEXT_LIST f} is a function that transforms a
window stack. It supplies {f} with the context of the window stack as
its list of theorems.

\ENDDOC
