\DOC UNDO_WIN

\TYPE {UNDO_WIN : (win_stack -> win_stack)}

\SYNOPSIS
Pops the window stack.

\LIBRARY window

\DESCRIBE
{UNDO_WIN} is used to undo the opening of a subwindow and every
subsequent transformation performed on the subwindow. {UNDO_WIN} will
return the stack with the parent window unchanged.

\FAILURE
{UNDO_WIN} will fail no windows have been opened on the window stack.

\SEEALSO
UNDO

\ENDDOC
